<apex:page standardController="Flat__c">
    <apex:form >
        <apex:pageBlock title="Add Flat">
            <apex:pageBlockSection >
                <apex:inputField value="{! Flat__c.Name }"/>
                <apex:inputField value="{! Flat__c.Status__c }"/>
                <apex:inputField value="{! Flat__c.Building_Name__c }"/>
                <apex:inputField value="{! Flat__c.Asking_Price__c }"/>
                <apex:commandButton action="{! save }" value="Save" />
            </apex:pageBlockSection>
        </apex:pageBlock>
    </apex:form>
</apex:page>